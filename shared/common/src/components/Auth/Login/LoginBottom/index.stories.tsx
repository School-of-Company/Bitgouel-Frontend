import { Meta, StoryObj } from "@storybook/react";
import LoginBottom from ".";

export default {
  title: 'common/components/Auth/LoginBottom',
  component: LoginBottom,
} as Meta<typeof LoginBottom>

type Story = StoryObj<typeof LoginBottom>

export const Primary: Story = {}