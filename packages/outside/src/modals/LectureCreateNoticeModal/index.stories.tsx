'use client'

import LectureCreateNoticeModal from '.'
import type { Meta, StoryObj } from '@storybook/react'

export default {
  title: 'outside/LectureCreateNoticeModal',
  component: LectureCreateNoticeModal,
} as Meta<typeof LectureCreateNoticeModal>

type Story = StoryObj<typeof LectureCreateNoticeModal>

export const Primary: Story = {}
