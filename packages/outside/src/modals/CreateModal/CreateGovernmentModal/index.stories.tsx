'use client'

import { Meta, StoryObj } from '@storybook/react'
import CreateGovernmentModal from '.'

export default {
  title: 'outside/modals/CreateGovernmentModal',
  component: CreateGovernmentModal,
} as Meta<typeof CreateGovernmentModal>

type Story = StoryObj<typeof CreateGovernmentModal>

export const Primary: Story = {}