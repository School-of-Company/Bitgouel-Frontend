'use client'

import LectureCreateModal from '.'
import type { Meta, StoryObj } from '@storybook/react'

export default {
  title: 'outside/LectureCreateModal',
  component: LectureCreateModal,
} as Meta<typeof LectureCreateModal>

type Story = StoryObj<typeof LectureCreateModal>

export const Primary: Story = {}
